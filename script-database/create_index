CREATE INDEX idx_ticket_trip_id ON tickets(trip_id);

CREATE INDEX idx_trip_seat_trip_id ON trip_seats(trip_id);
CREATE INDEX idx_trip_seat_trip_id_status ON trip_seats(trip_id, status);
CREATE INDEX idx_trip_seat_trip_id_status_processing_expired_at ON trip_seats(trip_id, status, processing_expired_at);

CREATE INDEX idx_trip_history_trip_id ON trip_history(trip_id);